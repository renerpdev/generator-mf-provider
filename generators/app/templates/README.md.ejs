# <%= appName %>

This is a **React Microfrontend Application** generated using a custom Yeoman generator 🎉  
It's configured with **Rsbuild**, **Module Federation**, and **TypeScript**.

---

## 🚀 Getting Started

### 1. Install dependencies (Optional)
```bash
pnpm install
```

### 2. Run the application
```bash
pnpm dev
```

The application will be available at:  
👉 http://localhost:<%= port %>

---

## 🧱 Project Structure
```
<%= appName %>/
├── .vscode/
│   └── settings.json
│   └── extensions.json
├── src/
│   └── components/
│       └── About.tsx
│       └── Home.tsx
│   ├── App.tsx
│   ├── bootstrap.tsx
│   ├── main.css
│   ├── index.ts
│   └── env.d.ts
├── public/
│   └── favicon.png
├── .gitignore
├── .nvmrc
├── .biome.json
├── module-federation.config.ts
├── package.json
├── pnpm-lock.yaml
├── .env.local
├── .env.production
├── .env.example
├── README.md
├── rsbuild.config.ts
└── tsconfig.json
```
---

## 🧩 Module Federation

This microfrontend exposes the following module(s):

- ./App → Main entry point of the React application

It's configured in module-federation.config.ts:

```js
exposes: {
  './App': './src/App',
},
```

---

## 🧠 Shared Dependencies

This application shares these packages as singletons with the Shell:

- react
- react-dom
- react-router-dom

---

## 🧰 Scripts

| Command | Description |
|----------|--------------|
| pnpm dev | Start the application in development mode |
| pnpm run build | Build the application for production |
| pnpm preview | Preview the production build locally |

---

## 💡 Notes

- To register this microfrontend in your **Shell**, check or update the remotes.json file within your Shell project.
- Make sure the port configured in rspack.config.js matches your desired development port (<%= port %>).

---

### 🖖 Generated by:  
**Yeoman Generator – Scaffold Tool for Microfrontends**

```bash
yo mf-app
```

---

Happy coding! 💙